# Screenshots

Drop the four PNGs here (exact names — the root README links to them):

| file | what to capture |
|---|---|
| `sessions.png`   | the Sessions sidebar with a few chats showing different status dots |
| `chat.png`       | a chat with streamed markdown + a tool/permission card |
| `selectors.png`  | the header with the Modello / Ragionamento / Modalità selects open |
| `usage-mcp.png`  | the 📊 usage panel and/or 🧩 MCP status panel open |

Capture on macOS with ⌘⇧4. Keep them reasonably sized (< ~500 KB each).
After committing + pushing to `main`, they appear automatically in the README and
on the Marketplace listing (the README uses absolute raw.githubusercontent.com URLs).
